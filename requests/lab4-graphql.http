@host = http://localhost:5185
@key = dev-super-secret-key

### Query: work orders
POST {{host}}/api/graphql
X-API-KEY: {{key}}
Content-Type: application/json

{
  "query": "query { workOrders { id title status priority aircraft { id tailNumber model } } }"
}

### Mutation: create work order
POST {{host}}/api/graphql
X-API-KEY: {{key}}
Content-Type: application/json

{
  "query": "mutation { createWorkOrder(input: { aircraftId: 1, title: \"GraphQL test\", description: \"demo\", priority: Medium }) { id title } }"
}
